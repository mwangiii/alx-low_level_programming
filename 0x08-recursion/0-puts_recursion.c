#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void();
char _puts_recursion[] = "String";
{
	if (*s != '\0')
	{
		_putchar(*s);
		_puts_recursion(s + 1);
	}
	else
	{
		_putchar('\n');
	}
}
